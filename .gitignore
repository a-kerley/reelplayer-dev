.DS_Store
.wrangler
worker/.wrangler
worker/.dev.vars
worker/secret
node_modules

# Generated by `npm run build:css` from css/tailwind-src.css - Cloudflare's
# own Build command (dashboard-configured, see CLAUDE.md's "Tailwind /
# daisyUI" section) regenerates this on every deploy, so it no longer needs
# to be committed.
css/tailwind.css

# Local knowledge-graph output (graphify skill)
graphify-out/
